### 修复

- 实验支持在聊天记录窗口中使用 `右键菜单 > 划词搜索`。
- 实验支持在聊天记录窗口中使用 `右键菜单 > 图片搜索`。
- 图片搜索在聊天记录里拿不到普通聊天消息数据时，会尝试从右键目标附近提取 `appimg://` 图片地址并回退到旧版图片链接解析。
- 尝试恢复旧版 QQNT 下 `右键菜单` 模块的可用性。
- 右键菜单监听改为重新兼容旧版 `.q-context-menu` 行为，不再只依赖菜单容器的一次性处理标记。
- 新增轻量工具箱菜单项去重逻辑，按菜单标题判断是否已经插入，避免旧版 QQ 复用菜单节点时重复追加。
- 菜单项创建逻辑增加旧版兜底：优先克隆 QQ 自带菜单项，找不到可用菜单项时手动创建一个基础菜单项。
- 图片搜索增加旧版 `appimg://` 路径兜底：当新版 `picData/getPicUrl` 参数拿不到时，回退为从本地图片文件名拼接 `gchat.qpic.cn` 图片链接。
- 图片搜索现在优先使用新版 `picData`，失败或缺失时使用旧版路径解析方式。
- 修复消息列表异常卡顿的问题
- 修复 小程序分享转为url卡片 功能默认开启无法关闭的问题

> [!NOTE]
> 由于 QQ 高版本已彻底调整界面布局，当前版本暂不兼容最新版本。本分支功能测试基于 QQ 9.9.20-37051 + LiteLoaderQQNT 1.2.4。
> 部分功能因新版本无法获取特定参数或受其他限制，仍暂不可用。  
> 这些功能如仍不可用，请优先确认 QQ 版本与功能限制。
