### 修复

- 划词搜索支持在设置页选择常用预设搜索源，同时保留自定义链接。
- 图片搜索支持在设置页选择常用预设搜索源，同时保留自定义链接。
- 搜索源默认不自动填写，首次使用需要选择预设或手动输入链接。
- 合并聊天记录窗口的右键菜单兼容修复。
- 支持在聊天记录窗口中使用 `右键菜单 > 划词搜索`。
- 实验支持在聊天记录窗口中使用 `右键菜单 > 图片搜索`。
- 右键菜单监听改为在渲染进程通用入口初始化，避免聊天记录页面没有加载聊天页模块时右键菜单不生效。
- 图片搜索在聊天记录里拿不到普通聊天消息数据时，会尝试从右键目标附近提取 `appimg://` 图片地址并回退到旧版图片链接解析。
- 修复旧版图片搜索路径解析，兼容 Windows 反斜杠路径，并且只移除文件名末尾的 `_0`。
- 不再做图片链接有效性预校验，避免右键搜索时卡住十几秒才打开浏览器。
- 仓库和更新入口改为 `yun-xiao1/lite-tools`。

### 发布

- Release 自动检查压缩包内容，缺少 `manifest.json`、`package.json`、`LICENSE`、`dist/main.js` 或 `src/renderer.js` 时会直接失败。
- Release 同时发布 `lite_tools_v4.zip.sha256`，用于校验下载文件完整性。
- README 增加 GitHub 官方下载、SHA256 和备用镜像下载入口。

### 环境

- QQNT：`9.9.20-37051` x64
- LiteLoaderQQNT：`1.2.4`
- 基于原版轻量工具箱：`2.33.14`

### 说明

- 聊天记录窗口的图片搜索属于实验支持，不保证每张图都能搜。
- 如果 QQ 图片 CDN 链接或 `rkey` 过期，Google Lens / SauceNAO 仍可能提示该网址中没有图片。
- 本 fork 优先保证旧版 QQNT 环境可用，不保证最新 QQNT 兼容。
